version: '3.8'

services:
  whatsapp-langgraph-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agente_participa
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    restart: unless-stopped
